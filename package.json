{
  "name": "seinca-enterprise",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "SEINCA Enterprise - presupuestos, APU trazables y comité híbrido de IA para construcción.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check app.js && node --check lib/apu-core.js && node --check api/gemini.js && node --check api/health.js && node --check api/pdf.js && node --check sw.js && node scripts/validate.mjs",
    "test": "node --test tests/*.mjs",
    "qa": "npm run check && npm test"
  },
  "dependencies": {
    "pdf-lib": "1.17.1"
  }
}
